package discult.modelapi.client.loaders.collada;

import discult.modelapi.client.loaders.collada.data.ColladaModel;
import net.minecraft.util.Identifier;

public class ColladaLoader
{
    public static ColladaModel loadColladaModel(Identifier loc)
    {
       // XmlNode node = XmlParser.loadXmlFile(loc);

       // SkinLoader skinLoader = new SkinLoader(node.getChild("library_controllers"), maxWeights);
        //SkinningData skinningData = skinLoader.extractSkinData();

        //GeometryLoader g = new GeometryLoader(node.getChild("library_visual_scenes") skinData.boneOrder);

        return null;
    }

}
